---
name: wallpaper-notes
description: Wallpaper_Notes - 桌面便签应用。用户在此项目的 notes/ 目录中创建 Markdown 文件，内容即可显示在 Windows 桌面壁纸层上。适合每日待办、金句书摘、灵感记录、工作备忘等场景。
---

# 📋 Wallpaper_Notes — 桌面便签

## ⚙️ 首次配置

**📂 记录笔记目录位置（项目运行时自动监听）：**
```
Wallpaper_Notes/notes/
```

将此目录下的 `.md` 文件视为「便签」，创建或修改即可实时呈现在桌面上。目录不存在时应用会自动创建。

## 🧩 这是什么

Wallpaper_Notes 是一个基于 PySide6 的 **Windows 桌面便签应用**。用户在该目录中编写 Markdown 文件，内容会自动显示在桌面壁纸层（Always-on-Desktop），无需手动打开窗口。

**💡 核心逻辑：写 Markdown = 贴便签。**

## ✍️ 支持的内容格式

| 语法 | 效果 |
|------|------|
| `## 标题` | 标题行（h1~h6 均可，h2 最常用），**无需空行分隔**，配置中可独立设色 |
| 段落文字 | 正文，单个换行即换行显示，空行分段 |
| `- xxx` / `1. xxx` | 📋 无序 / 有序列表（支持多级缩进嵌套） |
| `- [ ]` / `- [x]` | ✅ 任务列表（渲染为 ☐ / ☑） |
| `> xxx` | 💬 引用块（支持多行与嵌套） |
| `` `` ``` `` `` | 💻 围栏代码块（\`\`\`python 可标注语言） |
| `\| a \| b \|` | 📊 表格（表头行 + `\|---\|` 分隔行，GFM 管道语法） |
| `---` | ➖ 分隔线 |
| `**粗体**` | **粗体强调** |
| `*斜体*` | *斜体* |
| `~~删除线~~` | ~~删除线~~ |
| `` `代码` `` | `行内代码` |
| `[文字](网址)` | 🔗 可点击链接（点击后系统浏览器打开） |
| `![说明](路径)` | 🖼️ 图片（相对路径基于该便签所在文件夹解析；**超宽自动缩到便签宽度**）。可选尺寸：`{width=300}`（像素）或 `{width=50%}`（按当前便签宽度定成固定尺寸：窗口缩放不再等比变化，便签窄到放不下时才自动缩小；不会放大超过原图）。**同一行连写多张即并排**，放不下自动换行，如 `![a](1.png){width=48%} ![b](2.png){width=48%}` |

### 😊 Emoji 支持

便签内容 **完全支持 Emoji** 🎉。可以在笔记中自由插入各种 emoji 来装饰标题、标注重点或美化排版：

```markdown
## 📋 今日待办

- ✅ 完成报告
- 📞 回复客户电话
- 💻 写代码
- 🛒 买菜
```

实际显示效果（截取自桌面）：

> ✅ 完成报告
> 📞 回复客户电话
> 💻 写代码

系统字体回退策略会自动使用 Windows 原生 Emoji 字体（Segoe UI Emoji）渲染，无需额外配置。

## 🎯 使用场景

- 📋 **每日待办**：写一个 `每日待办.md`，列今天要做什么
- 📖 **金句/书摘**：比如 `挪威的森林.md`，放喜欢的句子在桌面上
- 💡 **灵感记录**：随时把想法写成文件，桌面自动显示
- 📌 **工作备忘**：重要事项、截止日期、会议要点
- 📅 **周计划/日计划**：简单的计划列表
- ⏰ **提醒事项**：喝水、吃药、定时任务等

## 🛠️ 便签操作

### ✨ 创建便签

在 `notes/` 目录下新建一个 `.md` 文件即可：

```bash
# 示例
echo -e "## 📋 我的待办\n\n- 完成报告\n- 回复邮件" > notes/我的便签.md
```

### ✏️ 修改便签

直接编辑已有 `.md` 文件，应用会自动检测变化并刷新显示。

### 🗑️ 删除便签

删除 `notes/` 下的 `.md` 文件，应用自动移除对应标签页。

## 🗂️ 分组（文件夹）

`notes/` 下的**子文件夹即分组**，可任意嵌套层级，桌面自动呈现为可展开的「抽屉」：

```bash
# 在分组中新建便签（分组不存在会随写入自动创建）
mkdir -p "notes/工作/2026"
echo "## Q3 计划" > "notes/工作/2026/Q3计划.md"

# 移动便签到分组（= 移动文件）
mv "notes/草稿.md" "notes/工作/周报草稿.md"

# 列出全部分组
find notes -type d -not -path notes
```

分组在标签行中是一颗淡底胶囊：悬浮或点击展开子项，点分组名收起；
打开组内便签时祖先抽屉自动展开。整理便签分类时直接移动文件即可。

## ⚠️ 注意事项

- 🔤 文件使用 **UTF-8 编码**
